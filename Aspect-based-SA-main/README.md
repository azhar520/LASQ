### LASQ

1. Put the formatted dataset into the `data/dataset` path.

2. Modify the `confing.yaml` configuration file in the `src` directory

3. Run the main file, using Uzbek as an example.
`python main.py --lang uz`